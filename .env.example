# URL pública do backend FastAPI.
# OPCIONAL: a URL de produção já está embutida como fallback em src/lib/api-client.ts
# (https://algolab-backend-0hao.onrender.com). Defina aqui apenas para sobrescrever
# (ex.: apontar para outro backend de teste). Deixe vazio para usar o fallback:
# - localhost:8000 quando rodando em dev local
# - URL do Render em produção
VITE_API_URL=https://algolab-backend-0hao.onrender.com
